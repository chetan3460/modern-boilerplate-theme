export default class MobileFilter {
  constructor(element) {
    this.element = element;
    this.init();
  }

  init() {
    // Set up event listeners for mobile filter functionality
    this.setupEventListeners();
  }

  setupEventListeners() {
    // If you need any additional event listeners not handled by Alpine
    // Or to interact with other components
  }
}